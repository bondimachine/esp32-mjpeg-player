#define WIFI_SSID "yourSSID"
#define WIFI_PASS "yourPassword"
#define PORT 1234
// #define ROTATE_180 1
// #define MPEG 1  // use MPEG1 video instead of MJPEG photo stream
// #define MPEGTS  // use MPEGTS as mux / transport. default if using MPEG, but it also upports MJPEG over MPEGTS
